name: Project Picori Release Build

on:
  push:
    tags:
      - 'v*'
  # Manual trigger so we can cut a Linux-only or Windows-only release
  # (e.g. v0.1.6.2 fixed Linux audio without re-shipping Windows). For a
  # full multi-platform release, just push the tag normally — this
  # dispatch path doesn't push a git tag, so the auto-trigger above
  # won't fire when this is used.
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag name to attach the release to (e.g. v0.1.6.2-experimental). Will be created at HEAD if missing."
        required: true
        type: string
      os_filter:
        description: "Which platform to build for"
        required: true
        type: choice
        default: all
        options:
          - all
          - x86
          - linux
          - linux-arm
          - windows
          - windows-arm
          - arm
          - macos
          - macos-arm
          - macos-x86
          - android

# Least privilege: the reusable build job only needs read; the release-
# creation job below elevates to contents: write for just its own scope.
permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/_build.yaml
    with:
      publish_mode: release
      # workflow_dispatch supplies inputs.tag; tag-push uses github.ref_name.
      version_label: ${{ inputs.tag || github.ref_name }}
      # tag-push always builds the full matrix (inputs.os_filter is empty
      # under push triggers, _build.yaml's default 'all' kicks in).
      os_filter: ${{ inputs.os_filter || 'all' }}
    secrets:
      ASSETS_TOKEN: ${{ secrets.ASSETS_TOKEN }}
      PRIVATE_SUBMODULES_TOKEN: ${{ secrets.PRIVATE_SUBMODULES_TOKEN }}
      TMC_DISCORD_APP_ID: ${{ secrets.TMC_DISCORD_APP_ID }}
      ANDROID_KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_B64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}

  create-release:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          path: release-assets

      - name: Flatten artifact files
        shell: bash
        run: |
          # The matheo merge in 3ad31b84 switched _build.yaml's archive
          # step from .zip to .tar.gz ("works on all hosted runners");
          # the v0.3.0-experimental release CI surfaced that this step
          # never got the matching update. Match both so a future
          # platform-specific .zip still works. .apk is the Android
          # release artifact (built by _build.yaml's build-android job).
          mkdir -p uploads
          find release-assets -type f \( -name '*.tar.gz' -o -name '*.zip' -o -name '*.apk' \) \
              -exec cp {} uploads/ \;

      - name: Create GitHub release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          name: "Project Picori ${{ inputs.tag || github.ref_name }}"
          files: |
            uploads/*.tar.gz
            uploads/*.apk
          generate_release_notes: true
          # A filtered dispatch (e.g. linux-arm only) produces no .apk;
          # the Flatten step above already fails if nothing was built.
          fail_on_unmatched_files: false
